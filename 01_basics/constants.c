#include <stdio.h>

int main() {
    const int id = 12;
    const int age = 18;
    const float fee = 85.15;
    const char grade = 'B';

    printf("ID: %d\n", id);
    printf("Age: %d\n", age);
    printf("Fee: %.2f\n", fee);
    printf("Grade: %c\n", grade);
    
    return 0;
}